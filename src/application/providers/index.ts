/**
 * Providers Barrel Export
 *
 * Providers are adapters for external services (email, SMS, payment, etc.)
 * They abstract external dependencies and make them testable.
 *
 * Example providers:
 * - EmailProvider: Sends emails via SendGrid, SES, etc.
 * - PaymentProvider: Processes payments via Stripe, PayPal, etc.
 * - NotificationProvider: Sends push notifications
 * - StorageProvider: Handles file storage (S3, GCS, etc.)
 */

// Export your providers here:
// export { EmailProvider, type IEmailProvider } from './EmailProvider.js';
// export { StorageProvider, type IStorageProvider } from './StorageProvider.js';
