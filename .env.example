# ============================================
# APPLICATION
# ============================================
NODE_ENV=development
PORT=3000
GRPC_ENABLED=false
GRPC_PORT=50051
LOG_LEVEL=info
SERVICE_NAME=my-microservice
# SERVICE_VERSION is read from package.json

# ============================================
# SECURITY
# ============================================
# CORS (comma-separated origins for production)
CORS_ORIGINS=https://app.example.com,https://admin.example.com

# JWT Authentication (min 64 characters - generate with: openssl rand -hex 32)
JWT_SECRET=change-me-generate-a-random-secret-of-at-least-64-characters-here
JWT_EXPIRES_IN=1h
JWT_ISSUER=my-app

# Encryption (for sensitive data, optional - falls back to JWT_SECRET)
# ENCRYPTION_KEY=your-encryption-key-min-32-characters

# Rate Limiting
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=60000

# ============================================
# DATABASE (MySQL)
# ============================================
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=secret
DB_NAME=my_database
DB_CONNECTION_LIMIT=100
# Max queued connection requests when the pool is exhausted (0 = unlimited, not recommended)
DB_QUEUE_LIMIT=100
DB_CONNECT_TIMEOUT=10000
# Allow multiple SQL statements per query (keep false - stacked queries amplify SQL injection)
DB_MULTIPLE_STATEMENTS=false

# ============================================
# REDIS CACHE
# ============================================
REDIS_SERVER=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_VHOST=my_cache
REDIS_ENABLED=true
CORE_AUTO_FEATURES=true

# ============================================
# RABBITMQ (Optional)
# Queue naming: {SERVICE_NAME}_{NODE_ENV}_{DEVICE_ID}_{queue_type}
# Example: knowledge_development_macbook1_file_processing
# ============================================
RABBITMQ_ENABLED=false
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USERNAME=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_VHOST=/
# Device identifier for queue isolation (e.g., macbook1, server1, ci-runner)
RABBITMQ_DEVICE_ID=local
RABBITMQ_PREFETCH=10

# ============================================
# OBSERVABILITY
# ============================================
# OpenTelemetry (Jaeger, Grafana Tempo, etc.)
OTEL_ENABLED=false
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
# OTEL_EXPORTER_OTLP_HEADERS=Authorization=Bearer xxx

# Sentry Error Tracking
# SENTRY_DSN=https://xxx@sentry.io/xxx
# SENTRY_ENVIRONMENT=development
SENTRY_TRACES_SAMPLE_RATE=0.1

# ============================================
# MIGRATIONS
# ============================================
# Prefix for migration files (e.g., auth_20251208123456_add_table.ts)
# Change this to your service name: auth, settings, keywords, integrations, social, etc.
MIGRATION_PREFIX=myservice

# ============================================
# MISC
# ============================================
TIMEZONE=+00:00
SHUTDOWN_TIMEOUT_MS=30000
